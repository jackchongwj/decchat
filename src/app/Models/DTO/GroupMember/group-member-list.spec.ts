import { GroupMemberList } from './group-member-list';

describe('GroupMemberList', () => {
  it('should create an instance', () => {
    expect(new GroupMemberList(0,0,"","",[])).toBeTruthy();
  });
});
