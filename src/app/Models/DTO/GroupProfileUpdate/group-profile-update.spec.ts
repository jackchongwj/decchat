import { GroupProfileUpdate } from './group-profile-update';

describe('GroupProfileUpdate', () => {
  it('should create an instance', () => {
    expect(new GroupProfileUpdate()).toBeTruthy();
  });
});
